pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'auto-tester'

include 'qa-agent-server'
include 'infrastructure-analyzer-plugin'
